---
# Ansible collection dependencies for ansible-xrootd-core.
# Install with: ansible-galaxy collection install -r requirements.yml
collections:
  - name: ansible.posix
    version: ">=1.5.0"    # provides ansible.posix.firewalld (Rocky/RHEL firewall)

  - name: community.general
    version: ">=7.0.0"    # provides community.general.ufw (Ubuntu firewall)
