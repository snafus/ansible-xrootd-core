---
# Minimal inventory for local syntax checks.
# Uses ansible_connection: local so no SSH target is required.
# Not used by molecule — molecule manages its own inventory.
all:
  children:
    xrootd_servers:
      hosts:
        localhost:
          ansible_connection: local
          xrootd_data_path: /data/xrootd
